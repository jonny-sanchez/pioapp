export type TipoBoletaType = {
  id_tipo_boleta: number;
  name: string;
  userCreatedAt: number | null;
  userUpdatedAt: number | null;
  createdAt: string; // ISO date
  updatedAt: string; // ISO date
}